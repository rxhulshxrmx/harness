// Temporary stub — full implementation is Task 15.
import type { Session } from "./session.ts";
import type { Message } from "../aicore/types.ts";

export function appendToStore(_session: Session, _message: Message): void {}
