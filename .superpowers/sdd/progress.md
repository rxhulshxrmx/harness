Task 1: complete (commits 4b825dc..b24306f, review clean after launch.json fix)
Task 2: complete (commits b24306f..92eae93, review clean; adopted project-wide convention: relative imports use explicit .ts extensions, src/package.json scopes ESM for test execution, root package.json stays CJS-default for the bundled extension)
Open minor item for Task 20 (packaging): no .vscodeignore yet — vsce package could bundle src/ raw sources into the .vsix.
Task 3: complete (commits 92eae93..4ccca1c, review clean)
Task 4: code complete (commits 4ccca1c..d6a7044, review clean). M0 CHECKPOINT PENDING human manual verification before Task 5.
Task 4 M0 checkpoint: DEFERRED — user has no SAP AI Core service key yet, live verification not possible. Proceeding with implementation; all manual/live-deployment checkpoints (M0-M4, tasks 4/9/12/14/16/17/19/20) deferred to user's own pass once credentials are available. Continuing task execution without pausing for GUI checks.
Task 5: complete (commits d6a7044..4a0bc67, review clean). Note: resolved a plan authoring bug — Task 5's brief test vs implementation code disagreed on whether tool_call function name length counts toward estimateTokens; implementer correctly followed the test (arguments only), disclosed in report.
Task 6: complete (commits 4a0bc67..de82475, review clean). Two plan-bug fixes verified sound: (1) getWorkspaceRoot uses lazy require("vscode") instead of top-level import, since index.test.ts imports pure helpers from the same file and a top-level vscode import crashes under plain node --test; (2) formatFileContent line-number padding width uses Math.max(2, String(total).length) instead of String(end).length, since the brief's original formula produced zero-padding (failing its own test regex) for single-digit line numbers.
Discovered systemic risk from (1): src/aicore/client.ts (Task 3, approved) also has a top-level `import * as vscode from "vscode"`, and Task 17's compaction.test.ts will transitively import client.ts (via compaction.ts's `import { chat as realChat } from "../aicore/client.ts"`), which will crash the same way. Proactively fixing client.ts now to use the same lazy-require pattern, before Task 17 hits it.
Task 6b (controller-initiated fix): complete (commit de82475..759a415, review clean). client.ts now uses the same lazy require("vscode") pattern as tools/index.ts, preempting the crash Task 17's compaction.test.ts would otherwise hit.
Task 7: complete (commits 759a415..50b80de, review clean). Note: plan doc had 2 literal NUL bytes (not "\x00" text, actual binary nulls) embedded in Task 7's matchesGlob code block, corrupting it for grep/task-brief extraction. Implementer independently reconstructed working glob-matching logic; reviewer confirmed it works for the brief's example, flagged a narrow edge case (bare root-level filenames vs **/ prefix) as Minor. Isolated to Task 7 only, verified no other NUL bytes in the plan file.
Task 8: complete (commits 50b80de..702d77f, review clean). Lazy-require-vscode convention correctly applied to searchReplace.ts (planReplacement stays pure/testable). diffTracker.ts stub created (real implementation is Task 13).
Task 9: complete (commits 9311547..2eeb4c4, review clean). M1 checkpoint deferred (same reason as M0 — no live SAP AI Core deployment to test against). Core turn loop (runTurn/UiPort) implemented per spec §3, base system prompt implemented, temporary forge.runTurn dev command wired for later manual testing.
Task 10: bash.ts implemented (commit 1bdde89). Review round 1 found Critical: shell-metacharacter injection (&&, ;, |, `, $(, newline, bare &) let compound commands ride an allowlisted prefix past auto-approval with zero prompt — inherited from the spec's own reference code, not implementer-introduced, but fixed regardless since it's an exploitable safety gap. Fixed in 05a55c5 (hasShellMetacharacters gate + word-boundary-safe never-list matching). Review round 2 found the fix still missed process substitution (<(...), >(...)) — same vulnerability class. Fixed in e359746. Final re-review in progress.
Task 10: complete (commits 2eeb4c4..f901691, review clean after 4 fix rounds — 05a55c5 shell-metacharacter injection, e359746 process substitution, cecbec3 prefix word-boundary, f901691 bare-redirection self-sufficiency). All 4 were real, live bypasses of the auto-approval allowlist found by adversarial reviewer execution, not just reading. Final state: hasShellMetacharacters gates isAutoApproved first (covers ;/&&/||/|/`/$(/<(/>(/newline/bare&/bare>), AUTO_APPROVE_PREFIXES requires whitespace-or-end boundary, READ_ONLY_EXACT/READ_ONLY_PREFIX_WORDS confirmed structurally immune to this bug class (exact-match/exact-token, not substring). 38/38 tests passing.
Task 11: complete (commits a745229..11665c2, review clean). Byte-for-byte match to brief, thorough XSS audit found zero innerHTML-with-dynamic-content paths (all textContent/createElement). Reviewer flagged revertFile as unwired — false alarm, confirmed against the plan: Task 14 Step 2 deliberately adds the revert button to main.js later; Task 11 only wires openDiff by design.
Task 12: complete (commits c3d6c66..a0d1b5b, review clean after 1 fix round). M2 checkpoint deferred (no live deployment). Note: implementer subagent was cut off twice by transient connection errors mid-task; controller inspected its completed-but-uncommitted work, verified it directly, and committed it on the implementer's behalf. Review found one Critical bug (Stop didn't resolve a pending approval, hanging the turn forever) — fixed and verified (traced end-to-end through bash.ts/loop.ts/panel.ts). "selectSession" unhandled was flagged but confirmed to be intentionally deferred to Task 16 per the plan, not a defect.
User requested renaming the extension from "Forge" to "Harness" throughout (2026-07-31), after Tasks 1-12 were built under the "Forge" name. Doing a full rename pass across source + plan doc before continuing to Task 13, so Tasks 13-20 are built with correct naming from the start.
Rename complete: Forge -> Harness across all source files (commit 907d79b), plan doc content (commit 7583d17), and plan filename (commit 77ec535, now docs/superpowers/plans/2026-07-29-harness-vscode-agent.md). Verified via tsc --noEmit, full test suite (38/38), and esbuild build after each rename step. package.json name/displayName now "harness"/"Harness". All forge.* config keys and command IDs are now harness.*. Resuming Task 13 under the new plan filename.
Task 13: complete (commits 68e2209..2aa4941, review clean). Real diffTracker.ts implemented (beginTurn/snapshot/endTurn/getSnapshot, first-write-wins via Map.has(), BeforeContentProvider using typed lazy-require-vscode pattern). loop.ts restructured with try/catch/finally so showTurnDiff fires exactly once per turn on every exit path. bash.ts records git-diff-fallback touched files. Design note for Task 14: getSnapshot() returns undefined both for "never touched" and "touched via git fallback, no exact snapshot" — Task 14 must only call getSnapshot(file) for files already in endTurn()'s returned list, never as a general probe.
Task 14: complete (commits 697355f..f4599cd, review clean). harness-before content provider, harness.openDiff (vscode.diff or git.openChange fallback), harness.revertFile registered. Safety-critical null/string/undefined branching verified character-by-character (null=delete, string incl. ""=replace, undefined=warn/no-op) — no data-loss risk found. main.js touched-files bar now has per-file revert buttons, XSS-safe rendering confirmed.
Task 15: complete (commits 0bd9068..35072b8, review clean after 1 fix round). Real JSONL persistence under .harness/sessions/ (newSessionFilePath/appendToStore/listSessions/loadSession). Review found Important bug: HarnessPanel constructor called getWorkspaceRoot() unguarded, which could crash the entire extension's activate() if no workspace folder is open (a normal state). Fixed via tryCreateSessionFilePath helper (try/catch, degrades to no persistence rather than throwing). Noted for later: two other unguarded getWorkspaceRoot() calls exist (agent/loop.ts:49, agent/systemPrompt.ts:8) — lower severity since they only run mid-turn after a user already interacted with the panel, not at activation time. Worth a look during Task 19 (error toast/hardening pass).
Task 16: complete (commits a3cd019..6729573, review clean after 1 fix round). M3 checkpoint deferred (no live deployment). Session dropdown wired: sessionList populated from listSessions on construction (guarded via new tryListSessions() helper, mirrors Task 15's tryCreateSessionFilePath), selectSession loads a prior session, title persists via updateSessionTitle on first turn. Review found two Important fragility bugs (unguarded loadSession in selectSession; unguarded updateSessionTitle in startTurn that could permanently stick the panel in "streaming" state if it threw before runTurn was called) — both fixed with try/catch, verified control flow traced by hand.
Task 17: complete (commit a1a32dc). Real compaction.ts (chat-generated summary + last user message, compaction marker line, chatFn injectable for testing). Done directly by controller (not subagent-reviewed) after hitting a usage limit mid-dispatch; verified via tsc/tests(51/51)/build before committing.
Task 18: complete (commit f04b3b6). Real agentsMd.ts (AGENTS.md/CLAUDE.md injection, 8000-char cap, CLAUDE.md fallback). Done directly by controller, verified via tsc/tests/build.
Task 19: verification-only, no new code needed. Confirmed both remaining unguarded getWorkspaceRoot() calls (loop.ts, systemPrompt.ts) are safe — both execute inside runTurn's existing try/catch/finally, so a throw there is caught and surfaced via ui.showError, not a crash. Live network-kill and Windows-shell checks remain deferred to the user's manual pass (spec §12 acceptance tests, no live deployment available in this session).
Task 20: complete (commit 71a1832). Full automated acceptance pass: tsc clean, 51/51 tests, esbuild build produces dist/extension.js + dist/webview/*. Added .vscodeignore (previously-flagged gap from Task 2 review) — vsce package now ships only package.json/dist/media (8 files, ~21KB), no leaked src/docs/.superpowers. Downgraded @types/vscode 1.125.0->1.90.0 to match engines.vscode floor (confirmed via clean tsc --noEmit that no newer API is used) rather than raising the compatibility requirement, preserving broad VS Code-fork compatibility. Added repository field. Packaged successfully as harness-0.0.1.vsix.

ALL 20 PLAN TASKS CODE-COMPLETE. Remaining for the user: the full manual acceptance pass from spec §12 (all 7 acceptance tests) once a real SAP AI Core service key is available — every M0-M4 checkpoint was deferred throughout the build for this reason. See docs/superpowers/plans/2026-07-29-harness-vscode-agent.md Task 20 Step 3 for the checklist.
